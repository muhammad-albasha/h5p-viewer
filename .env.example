# NextAuth Configuration
# For production, set this to your full domain URL including the basePath
# Example: https://mathematik-didaktik.uni-wuppertal.de/h5p-viewer
NEXTAUTH_URL=http://localhost:3000/h5p-viewer

# Generate a secure secret for NextAuth (at least 32 characters)
# You can generate one with: openssl rand -base64 32
# This MUST be changed in production!
NEXTAUTH_SECRET=development-secret-key-change-in-production-12345678

# Database Configuration
DB_HOST=localhost
DB_PORT=3310
DB_USERNAME=root
DB_PASSWORD=saSsd21-asd112A!1e14
DB_DATABASE=h5p

# Application Configuration
BASE_PATH=/h5p-viewer

# Production Notes:
# 1. NEXTAUTH_URL must be the full production URL (https://your-domain.com/h5p-viewer)
# 2. NEXTAUTH_SECRET must be a secure random string of at least 32 characters
# 3. Database credentials should be secured and not use default passwords
