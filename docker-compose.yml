version: "3.8"
services:
  backend:
    build:
      context: ./backend
    ports:
      - "3500:3500"
    environment:
      - DB_HOST=mysql
      - DB_USER=root
      - DB_PASSWORD=H212T5sd-P!234
      - DB_NAME=h5p
      - JWT_SECRET=Gsada23-H21513-!Pss2142
    depends_on:
      - mysql
    restart: always

  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: H212T5sd-P!234
      MYSQL_DATABASE: h5p
    ports:
      - "3308:3306"
    restart: always

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    ports:
      - "8086:80"
    environment:
      PMA_HOST: mysql
    restart: always
